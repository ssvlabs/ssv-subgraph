{
  "name": "ssv-network",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node http://localhost:8020 ssv-network",
    "create-local": "graph create --node http://localhost:8020/ ssv-network",
    "remove-local": "graph remove --node http://localhost:8020/ ssv-network",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 ssv-network",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-ts": "0.35.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
